apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: financeflow
  labels:
    app: analytics-service
    app.kubernetes.io/name: analytics-service
    app.kubernetes.io/part-of: financeflow
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
  selector:
    app: analytics-service
